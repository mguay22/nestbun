{
  "name": "nestbun-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*",
    "apps/*",
    "bench"
  ],
  "scripts": {
    "test": "bun test packages/platform packages/create-nest-bun/test",
    "typecheck": "bun run --filter '@nestbun/platform' --filter 'create-nest-bun' --filter '@nestbun/example-basic' typecheck",
    "build": "bun run --filter '@nestbun/platform' --filter 'create-nest-bun' build"
  },
  "devDependencies": {
    "bun-types": "1.4.2",
    "prettier": "3.9.9",
    "typescript": "7.0.2"
  }
}
